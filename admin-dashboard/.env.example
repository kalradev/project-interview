# API base URL of the interview backend (optional).
# If not set, the Vite dev server proxies /api to http://localhost:8000.
# For production or if the backend runs elsewhere, set e.g.:
# VITE_API_URL=http://localhost:8000
