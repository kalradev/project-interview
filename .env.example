# Optional: default API URL shown in the Interview Agent desktop app login screen.
# When building the installer, set this so candidates see your production API URL pre-filled.
# No domain needed – use your backend’s platform URL, e.g.:
# VITE_API_URL=https://your-project.railway.app
